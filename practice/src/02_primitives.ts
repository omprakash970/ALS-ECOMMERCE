let username:string='oppie';
let age:number = 19; 
let isStudent:boolean=true; 
const big:bigint= 2n**62n-1n;

const TOKEN:unique symbol = Symbol('token');

function yearsToDay(years:number):number{
  return years*365;
}